# Environment variable reference for local development.
# Do NOT create a .env file — use platform CLI authentication instead:
#   gcloud auth application-default login
#
# Secrets are loaded from GCP Secret Manager at runtime.
# Set these environment variables directly in your shell or via direnv.

# OpenAI API Key (required for completions and embeddings)
# OPENAI_API_KEY="sk-your-openai-api-key-here"

# Anthropic API Key (optional, for Claude models)
# ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key-here"

# Google API Key (optional, for Gemini models)
# GOOGLE_API_KEY="your-google-api-key-here"

# Default LLM Settings (optional, uses sensible defaults)
# DEFAULT_MODEL="gpt-4o"
# DEFAULT_TIMEOUT=60.0
# DEFAULT_MAX_RETRIES=3

# Retry Configuration (optional, for exponential backoff)
# RETRY_MIN_WAIT=1.0      # Minimum wait between retries (seconds)
# RETRY_MAX_WAIT=60.0     # Maximum wait between retries (seconds)
# RETRY_MULTIPLIER=2.0    # Exponential backoff multiplier

# Langfuse Configuration (optional, for observability)
# Note: Requires Langfuse v2.x due to LiteLLM compatibility
# LANGFUSE_SECRET_KEY="sk-lf-your-secret-key-here"
# LANGFUSE_PUBLIC_KEY="pk-lf-your-public-key-here"
# LANGFUSE_HOST="https://cloud.langfuse.com"

# OTLP Configuration (optional, for metrics export)
# OTLP_ENDPOINT="http://localhost:4317"
# IS_OTLP_INSECURE=true
# SERVICE_NAME="progression-labs-llm"
# SERVICE_ENVIRONMENT="development"
